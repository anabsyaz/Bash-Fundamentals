#!/bin/bash

# This script demonstrates how two-dimensional arrays work.
# Two-dimensional arrays aren't natively supported in bash.
# We will logically associate two arrays together 

# A 2D array is essentially an array of arrays.
# It is used to store data in a more structured way, similar to a table or matrix.
# Each element of the main array can be another array, allowing us to create a grid-like structure.
# For example, we can create a 2D array to represent a simple table of data
# where each row is an array of values.

